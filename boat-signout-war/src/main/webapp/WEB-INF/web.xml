<?xml version="1.0" encoding="UTF-8"?>
<web-app version="3.1" xmlns="http://xmlns.jcp.org/xml/ns/javaee" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd">
  <security-constraint>
    <web-resource-collection>
      <web-resource-name>AppStats</web-resource-name>
      <url-pattern>/appstats/*</url-pattern>
    </web-resource-collection>
    <auth-constraint>
      <role-name>admin</role-name>
    </auth-constraint>
  </security-constraint>
  <security-constraint>
    <web-resource-collection>
      <web-resource-name>UserSecurity</web-resource-name>
      <url-pattern>/*</url-pattern>
    </web-resource-collection>
    <user-data-constraint>
      <transport-guarantee>CONFIDENTIAL</transport-guarantee>
    </user-data-constraint>
  </security-constraint>
  <context-param>
    <param-name>user-base-url</param-name>
    <param-value>/user/admin</param-value>
  </context-param>
  <context-param>
    <param-name>static-base-url</param-name>
    <param-value></param-value>
  </context-param>
  <filter>
    <filter-name>guiceFilter</filter-name>
    <filter-class>com.google.inject.servlet.GuiceFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>guiceFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
    <dispatcher>FORWARD</dispatcher>
    <dispatcher>INCLUDE</dispatcher>
  </filter-mapping>
  <listener>
    <listener-class>org.apache.shiro.web.env.EnvironmentLoaderListener</listener-class>
  </listener>
  <listener>
    <listener-class>com.cilogi.shiro.guice.ServeContextListener</listener-class>
  </listener>
  <mime-mapping>
    <extension>manifest</extension>
    <mime-type>text/cache-manifest</mime-type>
  </mime-mapping>
  <!-- Keep members logged in for 90 days so they rarely have to re-authenticate.
       Two things are needed for that:
       (1) session-timeout: how long the SERVER keeps the session alive while idle.
           In minutes: 90 days = 90 * 24 * 60 = 129600.
       (2) cookie-config/max-age: makes the session cookie PERSISTENT on the client so
           it survives the browser being closed (without this it's a "session cookie"
           that dies on browser exit, forcing a re-login). In seconds: 90 days = 7776000.
       http-only keeps the cookie away from JavaScript; secure restricts it to HTTPS
       (the app already forces HTTPS via the CONFIDENTIAL transport-guarantee above).
       Removing access does NOT depend on these expiring: deleting a user blocks them on
       their next request, and suspending a user is enforced per-request (see
       BaseServlet.getCurrentGaeUser). -->
  <session-config>
    <session-timeout>129600</session-timeout>
    <cookie-config>
      <http-only>true</http-only>
      <secure>true</secure>
      <max-age>7776000</max-age>
    </cookie-config>
  </session-config>
  <welcome-file-list>
    <welcome-file>index.html</welcome-file>
  </welcome-file-list>
  
</web-app>
